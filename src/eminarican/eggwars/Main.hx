package eminarican.eggwars;

import pocketmine.plugin.PluginBase;

class Main extends PluginBase {

    override function onEnable() {
        getServer().getLogger().warning("eggwars");
    }

    override function onDisable() {
    }
}
